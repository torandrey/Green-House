<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Green House — база отдыха</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    html {
      scroll-behavior: smooth;
    }

    /* ======= ФОН ПЕРВОГО БЛОКА ======= */
    #main-block {
      background-image: url('img/главная/1.jpg');
      background-size: cover;
      background-position: center;
      height: 100vh;
      position: relative;
      font-family: sans-serif;
    }

    /* === АНИМАЦИЯ: Плавное появление с масштабированием (fade-in) === */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.8);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* === КРУГЛЫЙ ЛОГОТИП ПОД ТЕКСТОМ GREEN HOUSE === */
    #round-logo {
      width: 180px;
      height: 180px;
      margin: -40px auto;
      display: block;
      opacity: 0;
      animation: fadeIn 1.5s ease-out forwards;
      animation-delay: 0.5s;
    }

    /* ======= ПОЛУПРОЗРАЧНАЯ ПОЛОСА (фиксированная) ======= */
    #top-bar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 1.5cm;
      background-color: rgba(255, 255, 255, 0.7);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 20px;
      box-sizing: border-box;
      z-index: 1000;
    }

    /* ======= ССЫЛКА-ОБЕРТКА ДЛЯ ЛОГОТИПА ======= */
    #logo-link {
      display: flex;
      align-items: center;
      height: 100%;
    }

    /* ======= ЛОГОТИП ======= */
    #logo {
      height: 90%;
      width: auto;
      display: block;
    }

    /* ======= НАВИГАЦИОННЫЕ КНОПКИ ======= */
    #nav-buttons {
      display: flex;
      flex: 1;
      justify-content: space-evenly;
      align-items: center;
      margin-left: 20px;
    }

    .nav-button {
      font-weight: bold;
      font-size: 16px;
      background-color: rgba(0, 100, 0, 0.15);
      padding: 8px 16px;
      border-radius: 6px;
      text-decoration: none;
      color: #004400;
      transition: background 0.3s;
    }

    .nav-button:hover {
      background-color: rgba(0, 100, 0, 0.3);
    }

    /* ======= ТЕКСТ В ЦЕНТРЕ ======= */
    #main-title {
      position: absolute;
      top: calc(40% + 110px);
      left: 50%;
      transform: translate(-50%, -60%);
      text-align: center;
      color: green;
    }

    #main-title h1 {
      font-size: 46px;
      font-weight: 900;
      font-family: Georgia, serif;
    }

    #main-title p {
      font-size: 28px;
      font-weight: 500;
      color: #004400;
    }

    /* ======= БУРГЕР-МЕНЮ ДЛЯ МОБИЛОК ======= */
    #burger {
      display: none;
      flex-direction: column;
      cursor: pointer;
    }

    .burger-line {
      width: 25px;
      height: 3px;
      background-color: #004400;
      margin: 3px 0;
    }

    /* ======= АДАПТАЦИЯ ======= */
    @media (max-width: 768px) {
      #nav-buttons {
        display: none;
        position: absolute;
        top: 1.5cm;
        left: 0;
        width: 100%;
        flex-direction: column;
        background-color: rgba(255, 255, 255, 0.95);
        padding: 10px 0;
        z-index: 9;
      }

      #nav-buttons.active {
        display: flex;
      }

      #burger {
        display: flex;
      }
    }
  </style>
</head>
<body>

  <!-- === ПЕРВЫЙ БЛОК: ОБЕРТКА === -->
  <section id="main-block">

    <!-- === ПОЛУПРОЗРАЧНАЯ ПОЛОСА === -->
    <div id="top-bar">
      <!-- ЛОГОТИП (теперь кликабелен) -->
      <a href="index.html" id="logo-link">
        <img src="img/главная/logo.png" alt="Логотип" id="logo">
      </a>

      <!-- НАВИГАЦИОННЫЕ КНОПКИ -->
      <div id="nav-buttons">
        <a href="foto.html" class="nav-button">Фото</a>
        <a href="razmeshchenie.html" class="nav-button">Цены и размещения</a>
        <a href="uslugi.html" class="nav-button">Услуги</a>
        <a href="#contacts" class="nav-button">Контакты</a>
      </div>

      <!-- БУРГЕР-МЕНЮ -->
      <div id="burger" onclick="toggleMenu()">
        <div class="burger-line"></div>
        <div class="burger-line"></div>
        <div class="burger-line"></div>
      </div>
    </div>

    <!-- === ТЕКСТ В ЦЕНТРЕ === -->
    <div id="main-title">
      <h1 id="site-name">GREEN HOUSE</h1>
      <img src="img/главная/2.png" alt="Круглый логотип" id="round-logo">
      <p id="slogan">Отдых на Алтае</p>
    </div>

  </section>

  <!-- === СКРИПТ ДЛЯ БУРГЕРА === -->
  <script>
    function toggleMenu() {
      const nav = document.getElementById('nav-buttons');
      nav.classList.toggle('active');
    }
  </script>